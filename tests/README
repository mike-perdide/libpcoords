libpicviz tests: anything that can run the lib crazy goes here.

*any* file that at some point were source of a bug, crash, weird warnings *must* go here.

/
 |- history: pgdl files that caused some problems

